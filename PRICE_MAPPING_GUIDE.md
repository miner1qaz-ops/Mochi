# PRICE_MAPPING_GUIDE (Merged)

This guide has been merged into `PRICE_ORACLE_RUNBOOK.md`.

Canonical docs:
- `docs/TEMPLATE_STANDARD.md` (mainnet template identity: set/language/collector number/rarity/finish)
- `PRICE_ORACLE_RUNBOOK.md` (oracle runbook: mapping + bootstrap + scheduler)
- `docs/HANDOFF_PRICE_ORACLE.md` (temporary status; archive after mainnet cutover)

This file remains only as a legacy pointer and should be archived after mainnet cutover.
